#pragma once

enum class ERenderLayer
{
	BACKGROUND = -1000,
	COLMAP = -999,
	PLAYER = 0,
	FRONT = 10000,
};

enum class EPuyoColor
{
	Red,
	Green,
	Blue,
	Purple,
	Yellow,
	Garbage,
};

enum class EPuyoTextColor
{
	Red,
	Blue,
	Green,
};
